# Step 1：全域 CLAUDE.md — 語言與風格新增執行回饋格式規則

## 目標
在 `~/.claude/CLAUDE.md` 的 `## 語言與風格` 區塊末尾，新增一條「執行回饋格式」規則。

## 影響範圍
- 檔案：`C:\Users\terryhuang\.claude\CLAUDE.md`
- 影響所有專案、所有 session

## 具體修改

在以下現有內容之後：
```
- 格式：所有文件優先使用 Markdown (.md) 格式。
```

新增：
```
- 執行回饋格式：指令執行後，**只輸出**：
  1. **原因**：診斷分析（為什麼需要這樣改、根本原因）
  2. **結果**：改變了什麼（功能/邏輯層次描述，不含程式碼細節）
  不輸出：程式碼片段、diff、逐行說明、工具呼叫摘要。
```
